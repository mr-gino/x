X = [-1; -1]; 

W1 = [1.5  -2;
      0.5  -0.5;
      2     1];

b1 = [1; 1; 1];

f = @(x) (x >= 0);

net1 = W1 * X + b1;
h = f(net1);

W2 = [-3   2   -1;
      -2  -2    3];

b2 = [-0.5; -0.5];

net2 = W2 * h + b2;
y = f(net2);

disp(y)