X = [1; 2];

W1 = [1.5  -2;
      0.5   2];

b1 = [-2; 3];

sig = @(x) 1 ./ (1 + exp(-x));

net1 = W1 * X + b1;
h = sig(net1);

W2 = [-1  2;
      -1  0;
       0  3];

b2 = [-1; 2; -1];

net2 = W2 * h + b2;
y = sig(net2);

disp(y)